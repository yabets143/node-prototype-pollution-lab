<%- include('partials/header', { title: 'Admin • Parameter Pollution Lab' }) %>
<div class="row justify-content-center">
  <div class="col-12 col-lg-8">
    <div class="card">
      <div class="card-header"><strong>Welcome Admin</strong></div>
      <div class="card-body">
        <p class="text-secondary">This page intentionally renders a user-controlled template string.</p>
        <!-- render user-controlled string as EJS template -->
        <div id="bio-output" class="border rounded p-3"><%- ejs.render(user.bio || '', { require: require }) %></div>
      </div>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
